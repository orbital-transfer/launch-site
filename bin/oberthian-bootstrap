#!perl
# PODNAME: oberthian-bootstrap
# ABSTRACT: Oberth prototype bootstrap script

use strict;
use warnings;

use FindBin;
use lib "$FindBin::Bin/../lib";
use Oberth::Prototype::Command::Bootstrap;

sub main {
	Oberth::Prototype::Command::Bootstrap->new->run;
}

main;
