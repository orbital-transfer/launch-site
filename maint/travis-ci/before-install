#!/bin/sh

if [ "$TRAVIS_OS_NAME" = "linux" ]; then
	$OBERTH_PROTOTYPE_DIR/maint/build-docker
	docker ps -a
elif [ "$TRAVIS_OS_NAME" = "osx" ]; then
	$OBERTH_PROTOTYPE_DIR/maint/replace-shebang $OBERTH_PROTOTYPE_DIR/bin/oberthian

	oberthian bootstrap auto
fi
